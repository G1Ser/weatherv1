{
  "compilerOptions": {
    /* 基础配置 */
    "target": "ES2020",
    "module": "ESNext",

    /* 模块解析配置 - 修改为 node 以兼容 Webpack */
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "baseUrl": ".",

    /* 类型检查配置 */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,

    /* 输出配置 - Webpack 需要输出 */
    "sourceMap": true,

    /* 路径和库配置 */
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "paths": {
      "@/*": ["src/*"]
    },

    /* Vue 特定配置 */
    "jsx": "preserve",
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "forceConsistentCasingInFileNames": true,

    /* 类型声明 */
    "types": ["node", "webpack-env"]
  },

  /* 包含和排除的文件 */
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.vue",
    "types/**/*.d.ts"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
